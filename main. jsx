import React from "react";
import { BrowserRouter as Router, Routes, Route } from "react-router-dom";
import { 
  Shield, Lock, Bot, Brain, TrendingUp, 
  ArrowRight, Cpu, Users, Zap, Star, ShieldCheck, Globe
} from "lucide-react";

const fontElite = { fontWeight: '900', fontStyle: 'italic', textTransform: 'uppercase', letterSpacing: '-0.02em' };

const LandingPage = () => {
  return (
    <div style={{ backgroundColor: '#080808', color: 'white', minHeight: '100vh', fontFamily: "'Inter', sans-serif", margin: 0, position: 'relative', overflowX: 'hidden' }}>
      
      {/* FUNDO E GRID */}
      <div style={{ position: 'absolute', top: 0, left: 0, right: 0, bottom: 0, pointerEvents: 'none', backgroundImage: `linear-gradient(rgba(255, 255, 255, 0.015) 1px, transparent 1px), linear-gradient(90deg, rgba(255, 255, 255, 0.015) 1px, transparent 1px) `, backgroundSize: '50px 50px', zIndex: 0 }} />

      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@900&display=swap');
        
        .gradient-title {
          background: linear-gradient(90deg, #00FF95, #8B5CF6);
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          display: inline-block;
        }

        .glow-item {
          transition: all 0.4s ease;
          border: 1px solid rgba(255, 255, 255, 0.05);
          background: rgba(255, 255, 255, 0.03);
          backdrop-filter: blur(10px);
        }
        .glow-item:hover {
          border-color: #8B5CF6;
          box-shadow: 0 0 30px rgba(139, 92, 246, 0.25);
          transform: translateY(-5px);
          background: rgba(139, 92, 246, 0.05);
        }

        .section-card {
          background: rgba(255, 255, 255, 0.015);
          border: 1px solid rgba(255, 255, 255, 0.03);
          border-radius: 40px;
          padding: 60px;
          margin-bottom: 40px;
        }

        .btn-pill {
          background: linear-gradient(90deg, #00FF95, #8B5CF6);
          border: none;
          padding: 20px 50px;
          border-radius: 100px;
          color: black;
          font-weight: 900;
          font-style: italic;
          text-transform: uppercase;
          cursor: pointer;
          display: flex;
          align-items: center;
          gap: 15px;
          margin: 0 auto;
          transition: 0.3s;
        }
        .btn-pill:hover {
          box-shadow: 0 0 40px rgba(0, 255, 149, 0.4);
          transform: scale(1.05);
        }

        .text-purple { color: #8B5CF6; }
        .text-green { color: #00FF95; }
        .text-white { color: white; }
      `}</style>

      <div style={{ position: 'relative', zIndex: 1 }}>
        
        <nav style={{ display: 'flex', justifyContent: 'space-between', padding: '30px 60px', alignItems: 'center' }}>
          <h2 style={{ ...fontElite, fontSize: '22px', margin: 0 }}>
             <span className="text-white">AI</span> <span className="text-green">INVESTMENT</span>
          </h2>
          <button className="glow-item" style={{ background: 'transparent', color: 'white', padding: '10px 40px', borderRadius: '100px', fontWeight: '900', cursor: 'pointer', fontSize: '11px', border: '1px solid white' }}>
            LOGIN
          </button>
        </nav>

        <header style={{ textAlign: 'center', paddingTop: '100px', paddingBottom: '100px' }}>
          <h1 className="gradient-title" style={{ ...fontElite, fontSize: 'clamp(3.5rem, 12vw, 9.5rem)', lineHeight: 0.85, margin: 0 }}>
            <span style={{ color: 'white', WebkitTextFillColor: 'white' }}>AI</span> Investment
          </h1>
          <p style={{ color: 'rgba(255,255,255,0.6)', fontWeight: '700', letterSpacing: '1px', marginTop: '25px', fontSize: '20px' }}>
            Invista com Inteligência Artificial de última geração
          </p>
          <button className="btn-pill" style={{ marginTop: '50px' }}>
            CRIAR UMA CONTA AGORA <ArrowRight size={22} />
          </button>
        </header>

        <main style={{ maxWidth: '1250px', margin: '0 auto', padding: '0 20px' }}>
          
          <section className="section-card">
            <h2 style={{ ...fontElite, fontSize: '32px', marginBottom: '40px' }}>
              <span className="text-white">PASSO A</span> <span className="text-purple">PASSO</span>
            </h2>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))', gap: '25px' }}>
              <div className="glow-item" style={{ padding: '35px', borderRadius: '25px' }}>
                <div className="text-green" style={{ marginBottom: '20px' }}><Users size={40} /></div>
                <h3 style={{ ...fontElite, fontSize: '17px', marginBottom: '12px' }}>1. Cadastro</h3>
                <p style={{ fontSize: '14px', color: 'rgba(255,255,255,0.4)', lineHeight: '1.5' }}>Abertura de conta rápida com criptografia de dados.</p>
              </div>
              <div className="glow-item" style={{ padding: '35px', borderRadius: '25px' }}>
                <div className="text-green" style={{ marginBottom: '20px' }}><Zap size={40} /></div>
                <h3 style={{ ...fontElite, fontSize: '17px', marginBottom: '12px' }}>2. Deposito</h3>
                <p style={{ fontSize: '14px', color: 'rgba(255,255,255,0.4)', lineHeight: '1.5' }}>Depositos instantaneos via pix cartão de credito boleto ou gateway cripto.</p>
              </div>
              <div className="glow-item" style={{ padding: '35px', borderRadius: '25px' }}>
                <div className="text-green" style={{ marginBottom: '20px' }}><Brain size={40} /></div>
                <h3 style={{ ...fontElite, fontSize: '17px', marginBottom: '12px' }}>3. Ativar IA</h3>
                <p style={{ fontSize: '14px', color: 'rgba(255,255,255,0.4)', lineHeight: '1.5' }}>Defina seu perfil de investidor e ativa ia para começar a investir.</p>
              </div>
            </div>
          </section>

          <section className="section-card" style={{ background: 'rgba(139, 92, 246, 0.03)' }}>
            <h2 style={{ ...fontElite, fontSize: '32px', marginBottom: '15px' }}>
              <span className="text-white">NOSSA</span> <span className="text-purple">TECNOLOGIA</span>
            </h2>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))', gap: '25px' }}>
              {[
                { t: "Alta Performance", d: "Execuções em HFT para capturar as melhores taxas.", i: <Cpu /> },
                { t: "Conexão Global", d: "Monitoramento 24/7 dos maiores mercados do mundo.", i: <Globe /> },
                { t: "Auto-Evolução", d: "IA que aprende e se ajusta à volatilidade diária.", i: <Bot /> }
              ].map((tech, idx) => (
                <div key={idx} className="glow-item" style={{ padding: '35px', borderRadius: '25px' }}>
                  <div className="text-green" style={{ marginBottom: '20px' }}>{React.cloneElement(tech.i, { size: 35 })}</div>
                  <h3 style={{ ...fontElite, fontSize: '16px', marginBottom: '10px' }}>{tech.t}</h3>
                  <p style={{ fontSize: '13px', color: 'rgba(255,255,255,0.3)' }}>{tech.d}</p>
                </div>
              ))}
            </div>
          </section>

          <section className="section-card">
            <h2 style={{ ...fontElite, fontSize: '32px', marginBottom: '15px' }}>
              <span className="text-white">NOSSA</span> <span className="text-purple">SEGURANÇA</span>
            </h2>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))', gap: '25px' }}>
              {[
                { t: "Criptografia Militar", d: "Segurança AES-256 para todas as transações.", i: <ShieldCheck /> },
                { t: "Saque Prioritário", d: "Validação multi-fator para retiradas ultra-seguras.", i: <Lock /> },
                { t: "Auditoria Real", d: "Histórico de operações transparente e verificável.", i: <TrendingUp /> }
              ].map((sec, idx) => (
                <div key={idx} className="glow-item" style={{ padding: '35px', borderRadius: '25px', textAlign: 'center' }}>
                  <div className="text-green" style={{ marginBottom: '15px', display: 'flex', justifyContent: 'center' }}>{React.cloneElement(sec.i, { size: 45 })}</div>
                  <h3 style={{ ...fontElite, fontSize: '16px', marginBottom: '10px' }}>{sec.t}</h3>
                  <p style={{ fontSize: '13px', color: 'rgba(255,255,255,0.3)' }}>{sec.d}</p>
                </div>
              ))}
            </div>
          </section>

          <section style={{ padding: '60px 0' }}>
            <h2 style={{ ...fontElite, fontSize: '32px', textAlign: 'center', marginBottom: '50px' }}>
              <span className="text-white">COMENTÁRIOS DOS</span> <span className="text-purple">NOSSOS USUÁRIOS</span>
            </h2>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(320px, 1fr))', gap: '25px' }}>
              {[
                { u: "RICARDO A.", c: "A exclusividade desse sistema é o que mais me impressionou. Lucros consistentes e seguros." },
                { u: "BEATRIZ M.", c: "O suporte e a tecnologia da IA são de outro nível. Me sinto muito segura operando aqui." },
                { u: "FERNANDO S.", c: "Saques rápidos e interface premium. O melhor sistema que já utilizei até hoje." }
              ].map((val, i) => (
                <div key={i} className="glow-item" style={{ padding: '40px', borderRadius: '30px' }}>
                  <div style={{ display: 'flex', color: '#FFD700', marginBottom: '15px', gap: '4px' }}>
                    {[...Array(5)].map((_, i) => <Star key={i} size={14} fill="currentColor" />)}
                  </div>
                  <p style={{ fontStyle: 'italic', color: 'rgba(255,255,255,0.6)', fontSize: '14px', lineHeight: '1.6', marginBottom: '20px' }}>"{val.c}"</p>
                  <h4 className="text-green" style={{ ...fontElite, fontSize: '13px' }}>{val.u}</h4>
                </div>
              ))}
            </div>
          </section>
        </main>

        {/* RODAPÉ ATUALIZADO */}
        <footer style={{ padding: '60px 20px', textAlign: 'center', borderTop: '1px solid rgba(255,255,255,0.05)', marginTop: '40px' }}>
          <p style={{ ...fontElite, fontSize: '12px', color: 'rgba(255,255,255,0.5)', letterSpacing: '2px', margin: 0 }}>
            © 2026 TODOS OS DIREITOS RESERVADOS A <span className="text-green">AI INVESTMENT</span>
          </p>
          <p style={{ fontSize: '10px', color: 'rgba(255,255,255,0.2)', marginTop: '10px', fontWeight: '600' }}>
            PREMIER ELITE ACCESS // TECNOLOGIA QUANTITATIVA PROTEGIDA
          </p>
        </footer>

      </div>
    </div>
  );
};

export default function App() {
  return (
    <Router>
      <Routes>
        <Route path="/" element={<LandingPage />} />
      </Routes>
    </Router>
  );
}
